package murph.systemDesign.withoutStrategyPattern;

public class PassengerVehicle extends Vehicle{
    // Does not override the vehicle's drive function as it prefers implementation of parent class Vehicle
}
